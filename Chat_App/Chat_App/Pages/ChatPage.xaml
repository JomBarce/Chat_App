<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Chat_App.ChatPage"
             xmlns:controls="clr-namespace:Chat_App.CustomRenderers">
    <ContentPage.Content>
        <Grid HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand">
            <StackLayout HorizontalOptions="CenterAndExpand" VerticalOptions="StartAndExpand">
                <Grid Margin="10,10,10,0">
                    <Frame HasShadow="False" x:Name="SearchFrame" BorderColor="Black" CornerRadius="5"/>
                    <controls:BorderlessEntry x:Name="SearchEntry" Placeholder="Search user email" Keyboard="Email" 
                                              ClearButtonVisibility="WhileEditing" Completed="Search_Clicked"/>
                </Grid>
            </StackLayout>
            <Label Text="You have no contacts as of the moment." VerticalOptions="CenterAndExpand"
                   HorizontalTextAlignment="Center" TextColor="Black" FontSize="25"/>
        </Grid>
    </ContentPage.Content>
</ContentPage>